#!/usr/bin/env bash
# configures the container to fit 
# nginx must be running as nginx user
# nginx must be listening on all active IPs on port 8080
# You cannot use apt-get remove

chmod 744 /etc/nginx/nginx.conf
sudo -u nginx sed -i "s/listen 80 default_server/listen 8080 default_server/g" /etc/nginx/sites-available/default && service nginx start
